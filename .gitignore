# Binaries — anchored to repo root so we don't ignore cmd/titipdong/.
/titipdong
/titipdong.exe
*.exe
*.exe~
*.dll
*.so
*.dylib

# Local runtime
uploads/
*.log
.env
.env.*
*.env
!.env.example

# Node
node_modules/

# OS/editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Project spec / private notes
prompt.txt
*.prompt.txt

# Editor configs
.obsidian/

# Internal testing docs (not for public repo)
SKENARIO_TESTING.md
HASIL_TESTING_*.md

# Local deploy overrides (contain real IPs/credentials — never commit)
docker-compose.local.yml
.env.local
*.local.yml
j.txt
*.cookie
